# Active TODO

This is the active backlog for follow-up work.
It is fed by `.agents/planning/PLAN.md`.

Update rules:

- keep newest items at the top
- mark each item with a status tag
- add `owner`, `estimate`, and `blocked_by` when the follow-up is substantial
- move completed items to the evidence ledger when they are closed
- use `YYYY-MM-DD HH:MM` timestamps for new items so task age and cycle time can be estimated

## Current Items

1. `[done] 2026-03-26 01:58 - Split the large Vite main chunk` - the app bootstrap is split, the lesson registry is lightweight, and the warning is gone
2. `[done] 2026-03-26 01:58 - Decide whether the legacy archive docs need one final banner sweep` - added archive banners to `animator/agents.md` and `lessons/lesson-architecture.md`
3. `[done] 2026-03-26 01:45 - App shell moved under app/ with compatibility aliases` - closed in `app/index.html`, `app/main.js`, `index.html`, `main.js`
