# Active TODO

This is the active backlog for follow-up work.

Update rules:

- keep newest items at the top
- mark each item with a status tag
- move completed items to the evidence ledger when they are closed

## Current Items

1. `[follow-up] 2026-03-26 - Split the large Vite main chunk` - the build still warns about `appIndex` chunk size because of `piper`/`onnxruntime` payloads
2. `[follow-up] 2026-03-26 - Decide whether the legacy archive docs need one final banner sweep` - only if we want the frozen docs to be even more explicit
3. `[done] 2026-03-26 - App shell moved under app/ with compatibility aliases` - closed in `app/index.html`, `app/main.js`, `index.html`, `main.js`
