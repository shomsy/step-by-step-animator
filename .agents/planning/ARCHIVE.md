# Planning Archive

This file keeps legacy planning notes that are no longer active work.
The active plan lives in `.agents/planning/PLAN.md`.
The active backlog lives in `.agents/evidence/TODO.md`.
The evidence ledger lives in `.agents/evidence/CHANGELOG.md`.
